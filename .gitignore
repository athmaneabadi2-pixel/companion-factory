# Bytecode / venv / caches
__pycache__/
*.pyc
.venv/

# Secrets (ne jamais commit)
.env
**/.env

# DB locales
local.db
local.db-wal
local.db-shm

# On ignore toutes les instances...
/instances/

# ...SAUF les scripts d'ops de demo06 que l'on versionne
!instances/
!instances/demo06/
!instances/demo06/ops/
!instances/demo06/ops/*.bat
